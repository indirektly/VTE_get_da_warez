# VT Enterprise File Retreival Tool

This is used to download files from VT Enterprise from a search query.

It also allows you to do something with the files for ~reasons~


## Usage

```
$ pip install -r requirements.txt

$ 
```